// Cierra el modal
var cityModal = bootstrap.Modal.getInstance(document.getElementById('cityModal'))
cityModal.hide();

// Opcional: recarga la página para actualizar la lista
window.location.reload();
